spring:
  datasource:
    url: jdbc:mysql://localhost:3306/internship_db
    username: root
    password: "Chakri@2005"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true